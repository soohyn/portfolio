import { FC } from "react";

const About: FC = () => {
  return (
    <section id="about-section" className="bg-red-100 section-layout">
      <span>
        <pre>Hello, world!</pre>
        <pre>안녕하세요 프론트엔드 개발자 조수현 입니다.</pre>
      </span>
    </section>
  );
};

export default About;
